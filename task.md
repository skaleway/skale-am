Create a /scripts dir

create a file called run-dev.sh
the file should help setup the dev env.

if should do the following in the following steps.
install the deps `pnpm install`
build all of the packages in the packages dir using pnpm build
setup docker compose file with docker compose up -d
and start the dev env with pnpm dev
